={{ (() => { const cs = $('Current Scene').first().json; const r = Object.assign({}, cs.videoRequest); try { const a = $('Attach End Frame').first().json; if (a && a.endImage && a.sceneId === cs.id) r.endImage = a.endImage; } catch (err) {} try { const g = $('Submit Cooldown Guard').first().json; if (g && g.dropEndFrame && g.sceneId === cs.id) delete r.endImage; } catch (err) {} return r; })() }}
