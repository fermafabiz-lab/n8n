={{ { model: 'gpt-5.4', messages: [ { role: 'system', content: 'You are an expert script editor. Rewrite the provided script according to the producer\'s feedback, and make the result a story a viewer can follow easily: one throughline that moves forward, every fact, date, number and description stated ONCE in the whole script (never recap or re-establish earlier material), object inventories and one-to-four-word fragments turned into actions somebody takes, beats connected by cause and effect, no abstract commentary or morals mid-story. STRUCTURE IS A HARD RULE: your output must carry exactly the same [CHAPTER n: title] marker lines as the input \u2014 same count, same numbers, same order, same format, each alone on its line. The titles may change; the markers may not. Count the marker lines in the input and in your draft before you answer; if the two counts differ, fix the draft. A script that comes back without its markers cannot be cut into scenes. THEN: keep the same language as the original, keep the total word count within 10% of the original, never invent a fact that is not in the original, and output flowing spoken narration only — no markdown, no meta text, no explanations. Return ONLY the rewritten script.' }, { role: 'user', content: 'FEEDBACK FROM THE PRODUCER:\n' + ($('Fetch Script Status').first().json.fields['Observații Script'] || 'Improve pacing, concreteness and flow.') + '\n\nCURRENT SCRIPT:\n' + $('Fetch Script Status').first().json.fields['Script Content'] } ] } }}