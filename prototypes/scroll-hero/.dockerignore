# The frames and the poster are served by Caddy from the host, not by Next
# from public/. Leaving the whole directory out of the build context is what
# keeps them out of the image — and the Dockerfile therefore has no
# `COPY public`, because the directory will not exist here.
public

node_modules
.next
test-results
shots
lighthouse
tests
playwright.config.ts
scripts/lighthouse.mjs
README.md
.gitignore
Dockerfile
.dockerignore
