={{ (() => { const r = { email: 'fermafabiz@gmail.com', model: ($json.model || 'veo-3.1-lite-low-priority'), prompt: 'Single continuous shot, no cuts and no scene changes. Begin exactly on the given frame and play the action below as written, in the direction written. ' + $json.motionPrompt + ' Physics: solid things stay solid — vehicles, people and objects never pass through each other, never overlap and never sink into the ground; every moving vehicle has its driver (visible, or plausibly hidden by the bodywork); nothing floats, melts or morphs; nobody and nothing appears, disappears or duplicates. Backgrounds and camera move consistently with the subject, so parallax reads correctly. Audio: quiet natural ambient sound effects that match the scene only — no speech, no voices, no singing, no narration, no music, no soundtrack.', startImage: $json.imageId, aspectRatio: ($('Receive Batch Input').first().json.Aspect_Ratio === '9:16' ? 'portrait' : 'landscape'), seed: $json.seed, count: 1, async: true, captchaRetry: 1 }; try { const a = $('RG Attach End Frame').first().json; if (a && a.endImage && a.sceneId === $json.id) r.endImage = a.endImage; } catch (err) {} try { const g = $('Regen Cooldown Guard').first().json; if (g && g.dropEndFrame && g.sceneId === $json.id) delete r.endImage; } catch (err) {} try { const m = $('RG Motion Resubmit').first().json; if (m && m.sceneId === $json.id) { if (m.seed) r.seed = m.seed; if (m.dropEndFrame) delete r.endImage; } } catch (err) {} return r; })() }}
